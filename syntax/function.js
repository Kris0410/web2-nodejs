console.log(Math.round(1.6));
console.log(Math.round(1.4));

function sum(first, second){ // parameter
    return first + second;
}

var a = sum(1., 2);

this.sum(1, 1); // argument

// control + k + f == 코드 정렬